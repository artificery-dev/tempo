# Y2: keep the audio graph schedulable while software video decoding and
# Flutter rendering are active. A short notification PCM otherwise pulls
# the whole graph down to 128 frames and video/audio clocks lose real time.
# 512 frames at 48 kHz is 10.67 ms; larger client requests remain allowed.
context.properties = {
    default.clock.min-quantum = 512
}
