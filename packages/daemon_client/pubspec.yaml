name: daemon_client
publish_to: none
version: 0.1.0
resolution: workspace
environment:
  sdk: ^3.12.2
dependencies:
  player_api:
    path: ../player_api
dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
